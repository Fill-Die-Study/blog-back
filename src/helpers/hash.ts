// import bcrypt, { hash as _hash } from 'bcrypt';
// import { HASH_SALT } from 'src/constants';

// export default function hash(password: string) {
//   console.log('salt', HASH_SALT);
//   return _hash(password, HASH_SALT);
// }

// export function compare() {
//   bcrypt.compare
// }
